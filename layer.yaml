includes: ['layer:hadoop-client', 'interface:mysql']
options:
  hadoop-client:
    packages:
        - 'libmysql-java'
        - 'mysql-client'
    users:
      hive:
        groups: ['hadoop']
    dirs:
      hive:
        path: '/usr/lib/hive'
        owner: 'hive'
        group: 'hadoop'
      hive_conf:
        path: '/etc/hive/conf'
    ports:
      hive:
        port: 10000
        
